const express = require("express");
const router = express.Router();
// Word Spy game state is managed entirely via Socket.io.
// This router is a placeholder for any future REST endpoints (e.g., game history).
module.exports = router;
